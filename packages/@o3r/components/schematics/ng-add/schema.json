{
  "$schema": "http://json-schema.org/schema",
  "$id": "ngAddSchematicsSchema",
  "title": "Add Otter components ",
  "description": "ngAdd Otter components ",
  "properties": {
    "projectName": {
      "type": "string",
      "description": "Project name",
      "$default": {
        "$source": "projectName"
      }
    },
    "enableCms": {
      "type": "boolean",
      "default": false,
      "description": "Activate CMS compatibility",
      "x-prompt": "Add all the scripts to make the application CMS compatible?"
    }
  },
  "additionalProperties": false,
  "required": [
  ]
}
